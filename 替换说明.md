# v4.0.1 增量更新 · 只替换这 6 个文件

你已经传上 GitHub 的那版之后，我改了手机布局和帮助文档。
**只需要替换下面这几个文件，其他一律不用动。**

## 必须替换（2 个）

这两个是真正影响使用的：

| 文件 | 放到仓库的哪里 | 改了什么 |
|---|---|---|
| `settingsPanel.js` | `txtToWorldbook/ui/settingsPanel.js` | 温度和最大输出换到第二行，加自动折行和最小宽度，修手机上输入框被挤成两位数的问题；超时标签改名「无响应超时(秒)」，上限 600 → 3600 |
| `zh-CN.js` | `txtToWorldbook/locales/zh-CN.js` | ❓帮助里补上了新功能说明（导入酒馆预设、导入更新章节、流式输出、温度/最大输出、无响应超时含义），使用技巧新增 5 条 |

## 建议替换（2 个）

不影响功能，但版本号和更新记录会对不上：

| 文件 | 放到哪里 | 改了什么 |
|---|---|---|
| `package.json` | 仓库根目录 | 版本号 1.8.1 → 4.0.1（之前漏改了） |
| `CHANGELOG.md` | 仓库根目录 | 补上 v4.0.1 的完整更新记录（之前打包时丢失了） |

## 新增（2 个）

| 文件 | 放到哪里 | 说明 |
|---|---|---|
| `功能测试清单.md` | 仓库根目录 | 26 条测试项，标了每个功能在界面哪个位置 |
| `tools/smoke-boot.mjs` | `tools/smoke-boot.mjs`（覆盖原有的） | 加了 5 条布局断言。不装这个也行，只是本地验证会少测几项 |

---

## 网页上传怎么操作

替换单个文件比整包上传简单得多：

1. 在仓库里点进要替换的文件，比如 `txtToWorldbook/ui/settingsPanel.js`
2. 右上角 `⋯` → `Edit file`（或直接点铅笔图标）
3. **全选原内容删掉**（电脑 `Ctrl+A` 再删）
4. 把新文件内容整个粘贴进去
5. 底部 `Commit changes`

`功能测试清单.md` 是新文件，用 `Add file` → `Create new file`，文件名填 `功能测试清单.md`，粘贴内容再提交。

如果你用 GitHub Desktop，直接把这几个文件按对应路径覆盖进本地仓库，然后提交推送就行。

---

## 传完之后

在 SillyTavern 里点插件的更新按钮，然后浏览器 `Ctrl + F5` 强制刷新。

**先验证这两条**：

1. 打开主界面，看基本设置区——温度和最大输出应该在第二行了，手机上数字能完整显示
2. 点右上角 ❓ →「🔧 API 模式」，应该能看到「三家全部流式传输」「无响应超时」这些新条目

两条都对就说明替换成功，可以开始按《功能测试清单.md》一条条测了。

---

## 顺便一提：超时值建议改小

你之前填的 1200 秒。这个值现在的含义是「多久没收到新数据」，不是总耗时上限，所以**建议改成 120～300**。

填 1200 的副作用是：真正卡死的请求要等 20 分钟才会报错重试。而正常生成时，两个字之间的间隔通常不会超过几十秒。
