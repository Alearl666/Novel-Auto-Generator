# 新增功能测试清单 · v4.0.1

对比你原来那份单文件版（v3.2.1），下面是**全部新增和改动**，每条都标了在界面哪个位置、怎么测、预期看到什么。

建议按顺序测，前面的是基础，后面的依赖前面。测到哪条不对就停下来，把那条的编号和现象告诉我。

---

## 界面位置速查

打开主界面（扩展面板 → 📚 TXT转世界书）后，从上到下依次是：

```
┌─ 顶部 ────────────────────────────────
│  ❓帮助   ✕关闭
├─ 基本设置 ────────────────────────────
│  每块字数 │ 无响应超时(秒) │ [🔄重新分块]     ← 第1行
│  温度     │ 最大输出                        ← 第2行【新增】
├─ API 配置 ────────────────────────────
│  ☐ 使用酒馆API（默认不勾）【改动】
│  API提供商 / Key / Endpoint / 模型
├─ 提示词区（可折叠）───────────────────
│  📌 后缀提示词
│  💬 消息链配置  ← 【导入酒馆预设】在这里
│  🏷️ 自定义提取分类
├─ 📄 文件上传 ─────────────────────────
│  [📥合并世界书] [📗导入更新章节]【移植】
│  [📥导入任务]   [📤导出任务]
├─ 章节队列 ────────────────────────────
├─ 处理进度 ────────────────────────────
│  👁️ 实时输出  ← 【流式逐字显示】在这里
└─ 结果区 ──────────────────────────────
```

---

# A 组 · 基础确认（3 条）

### A1 · 插件能加载

**位置**：扩展面板
**操作**：打开扩展面板
**预期**：看到「📚 文件转换工具」，展开后有三个按钮——TXT转世界书、EPUB转TXT、世界书导出

不对的话说明文件没放全，后面都不用测了。

### A2 · 主界面能打开

**位置**：点「📚 TXT转世界书」
**预期**：弹窗正常出现，不白屏、不报错

### A3 · 版本号对

**位置**：扩展面板里插件名字旁边，或 `manifest.json`
**预期**：`4.0.1`

如果还是 3.0.8，说明浏览器用的是缓存，`Ctrl + F5` 强制刷新（手机上长按刷新选「清空缓存并硬性重新加载」）。

---

# B 组 · 手机布局修复（2 条）

### B1 · 温度和最大输出在第二行 【本次修复】

**位置**：基本设置区
**问题**：你反馈四个输入框挤在一行，每个只能看到两位数字
**现在**：改成两行——第一行「每块字数 + 无响应超时 + 重新分块按钮」，第二行「温度 + 最大输出」，并且加了自动折行和最小宽度

**预期**：手机上每个输入框都能完整看到数值，不再被挤成两位数

### B2 · 超时标签改名了 【本次修改】

**位置**：基本设置区第一行第二个
**原来**：`API超时(秒)`，上限 600
**现在**：`无响应超时(秒)`，上限 3600

**为什么改**：这个值的含义变了。原来是「总耗时上限」，跑够 600 秒就必挂；现在是「多久没收到新数据」，只要 AI 还在吐字就永远不会中断。

你之前填的 1200 现在是合法值了（原来 max=600，虽然能输入但属于越界）。

**建议值**：120～300 就够。这个数字不需要覆盖生成总时长，只需要覆盖「两个字之间最长的间隔」。填太大反而会让真卡死的请求拖很久才报错。

---

# C 组 · 超时与流式（4 条）—— 这组是你最初的问题

### C1 · 实时看到 AI 输出 【新增，最直观】

**位置**：处理进度区 → 「👁️ 实时输出」按钮
**操作**：传一个小 txt（一两章就行），点开始转换，然后展开实时输出面板
**预期**：能看到 AI 的字**一个一个蹦出来**，前面有一行 `💬 AI 正在输出：`

**对比**：你原来那版这个面板永远是空的——不是坏了，是代码里从来没往里写过东西。作者新版也一样，他留了回调接口但没接线。

### C2 · 并行时只跟一个任务

**位置**：同上
**操作**：开并行（默认 3 并发），跑一个多章节的文件
**预期**：只有**第一个开跑的章节**逐字显示，其余的只显示 `📤 [任务2] 发送请求...` 这类进度行

这是故意的（你选的方案 B）。三股字流挤进同一个面板会交错成乱码。

### C3 · 长时间生成不再超时 【核心修复】

**位置**：无固定位置，看行为
**操作**：把每块字数调大（比如 30000），让单章生成时间拉长到几分钟
**预期**：只要实时输出面板里还在出字，就不会中断

**原来的问题**：固定墙钟计时，跑够设定秒数必挂，跟有没有在传数据无关。

### C4 · 三家都能流式

**位置**：API 配置区 → API提供商
**操作**：分别切到 OpenAI兼容 / Gemini / Anthropic 各跑一章
**预期**：三个都能在实时输出面板看到逐字输出

**原来**：只有 OpenAI兼容是流式，Gemini 和 Anthropic 都是非流式（等全部生成完才一次性返回）。

---

# D 组 · 采样参数（3 条）

### D1 · 温度可调 【新增】

**位置**：基本设置区第二行
**操作**：改成 0.8，关掉主界面再打开
**预期**：值还是 0.8（说明存下来了）

**原来**：写死在代码里的 0.3，界面上没法改。

### D2 · 最大输出可调 【新增】

**位置**：基本设置区第二行
**操作**：填 8192，关掉再打开
**预期**：值还是 8192

**留空的含义**：用接口默认值（OpenAI兼容 64000 / Gemini 65536 / Anthropic 8192）。

**什么时候需要改**：中转报「超出最大长度」之类的错时，在这里填个小值。原来遇到这情况完全没辙。

### D3 · 温度填 0 不会被吃掉

**位置**：同上
**操作**：温度填 `0`，关掉再打开
**预期**：还是 `0`，不会变回 0.3

（这是个容易写错的地方，0 是合法值但很容易被当成"空值"处理掉。已加测试覆盖。）

---

# E 组 · 导入酒馆预设（4 条）【新增】

### E1 · 按钮在哪

**位置**：提示词区 → 展开「💬 消息链配置」→ 紫色按钮「📥 导入酒馆预设」

### E2 · 导入流程

**操作**：点按钮，选一个酒馆导出的预设 json
**预期**：先弹出解析摘要，长这样：

```
预设「你的预设名」解析结果：

· 条目 8 条（启用 6 条）
· 丢弃占位符 3 个（角色卡/世界书/示例对话，本插件无对应物）
· 跳过空内容 1 条
· 正文槽位：来自「聊天记录」占位符
· temperature = 1.05
· max_tokens = 4096

导入会覆盖当前消息链，确认？
```

确认后，消息链列表会按预设的顺序铺开。

### E3 · 采样参数被同步

**操作**：导入一个 temperature 不是 0.3 的预设，然后看基本设置区
**预期**：温度框的值变成了预设里的值

### E4 · 酒馆API模式下会警告

**操作**：先勾上「使用酒馆API」，再导入预设
**预期**：导入成功后额外弹一句警告，说明酒馆模式下角色会被压平、预设排布不生效

**这个警告是真的**，不是吓唬人。酒馆 API 走的是 `generateRaw`，system/assistant 角色会被拼成纯文本。

**已知限制**（摘要里也会告诉你）：
- 角色卡类占位符（角色描述、世界书、示例对话）会被丢弃，因为本插件没有角色卡
- 按深度注入的条目（多数越狱条目）近似放在正文之后，depth 大的靠前

---

# F 组 · 导入更新章节（3 条）【从你的版本移植】

### F1 · 按钮在哪

**位置**：📄 文件上传区的标题栏右侧，绿色按钮「📗 导入更新章节」

### F2 · 两种模式

**操作**：先正常传一个 txt 跑几章，然后点这个按钮
**预期**：弹出模式选择框，两个选项：

- **➕ 仅新增模式（推荐）**——导入的文件只含新章节，全部直接追加
- **📖 完整文件模式**——导入更新后的整本 TXT，自动识别新增部分

选完点「下一步 · 选择文件」，再选 txt。

### F3 · 不影响已处理的条目

**操作**：跑完几章后导入更新，然后看章节队列
**预期**：
- 新章节追加在末尾，编号连续
- 已处理的章节仍然是已处理状态
- 起始点自动落在第一个新章节上，点「开始/继续转换」只跑新增部分
- 已整理、已别名合并的世界书条目不会被刷新

**顺带修的一个坑**：完整文件模式定位新增内容时，原来用 `indexOf` 找锚点。如果小说里有大段重复文本（诗词、章节模板、固定开场白），会命中靠前那次，把已有的几千字当成新增再导一遍。改成了「预期位置 → lastIndexOf → 长度截取」三级定位。

---

# G 组 · 作者新版带来的功能（7 条）

这些是你原来那份没有、作者模块化版本里有的。

### G1 · 手动合并

**位置**：结果区 → 「🔗 手动合并」按钮
**作用**：自己挑两个条目合并，不依赖 AI 判断

### G2 · 增量导出

**位置**：结果区 → 「📤 导出变更」
**作用**：只导出上次导出之后新增/修改的条目

### G3 · Prompt 模板可视化编辑

**位置**：各提示词旁的「👁️ 预览」按钮
**预期**：弹出带四个标签页的编辑器（世界书 / 剧情 / 文风 / 预览），可以直接改完整模板

### G4 · 自动存盘

**位置**：无界面，后台行为
**操作**：处理到一半直接刷新浏览器，再打开主界面
**预期**：进度还在，能接着跑
**机制**：每 60 秒自动保存 + 关页面前兜底保存

### G5 · 剩余时间预估

**位置**：处理进度区
**预期**：跑几章之后出现「预计剩余 X 分钟」

### G6 · 历史版本逐行 diff

**位置**：「📜 修改历史」→ 选两个版本对比
**预期**：逐行显示增删，不再是整段替换

### G7 · JSON 截断自动修复

**位置**：无界面
**作用**：AI 输出被 max_tokens 砍断导致 JSON 不完整时，自动补齐括号、修中文引号、去尾逗号
**怎么验证**：把最大输出调很小（比如 1024）跑一章，原来会直接失败，现在大概率能救回来

---

# H 组 · 帮助文档补充（1 条）【本次修复】

### H1 · ❓帮助里现在有新功能说明了

**位置**：主界面右上角「❓」按钮
**问题**：你指出的——新功能没写进帮助
**现在补充了**：

- 🔧 API 模式 → 三家全流式、无响应超时的含义、温度/最大输出怎么用、酒馆API的限制说明
- 📝 提示词系统 → 导入酒馆预设的完整说明，包括哪些占位符会被丢弃、深度注入怎么处理
- 📋 章节管理 → 导入更新章节的两种模式区别
- 💡 使用技巧 → 新增 5 条，覆盖上面这些功能的常见问题

**验证**：点❓，翻到「🔧 API 模式」和「📝 提示词系统」，应该能看到上面这些内容。

---

# I 组 · 默认值和移除项（3 条）

### I1 · 默认走自定义API 【改动】

**位置**：API 配置区最上面的勾选框
**预期**：全新安装时「使用酒馆API」**不勾选**
**原来**：默认勾选

### I2 · DeepSeek 和 Gemini代理已移除

**位置**：API提供商下拉
**预期**：只有三个选项——OpenAI兼容 / Gemini / Anthropic

**如果你的旧设置里存着 deepseek**：不会报错，会自动落回 OpenAI兼容。

### I3 · Anthropic 通道能用了 【修复】

**位置**：API提供商 → Anthropic
**操作**：填 Anthropic 的 key，跑一章
**预期**：能正常出结果

**原来的问题**（作者版本里这个通道其实是坏的）：
- 缺了浏览器直连必需的 `anthropic-dangerous-direct-browser-access` 请求头，会被 CORS 拦掉
- system 消息直接塞进 messages 数组，但 Anthropic 不接受这种格式，必须提到顶层 `system` 字段

两个都修了。如果你不用 Anthropic，跳过这条。

---

# 出问题怎么报给我

按这个格式说，我能最快定位：

```
测到 [编号]，比如 C1
预期：能看到逐字输出
实际：面板还是空的
报错：（F12 控制台的红字，有就截图）
```

**F12 怎么开**：电脑上直接按 F12，选 Console 标签，找红色的行。手机上如果不方便，就描述现象，多数问题我能从现象推断。

---

# 一句话总结改了什么

| 类别 | 内容 |
|---|---|
| **修复你踩过的坑** | 长时间生成必超时 |
| **新增** | 实时逐字输出、导入酒馆预设、温度/最大输出可调 |
| **移植保留** | 导入更新章节、EPUB转TXT、世界书导出 |
| **顺手修的 bug** | Anthropic 通道、章节追加误判重复、采样参数不保存 |
| **本次修复** | 手机布局挤压、帮助文档缺失 |
| **白拿的** | 手动合并、增量导出、模板编辑、自动存盘、ETA、diff、JSON 修复 |
