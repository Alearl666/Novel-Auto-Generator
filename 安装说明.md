# 安装说明 · v4.0.1

先看一句话：**这个版本不再是单个 js 文件，而是一整个文件夹，必须整个替换。**
少放一个文件插件就打不开，所以别只挑几个文件覆盖。

> **想传到自己的 GitHub 仓库？** 看同目录下的 `更新到GitHub.md`。
> 传上去之后 SillyTavern 就能用仓库地址安装，以后更新点一下按钮就行，不用再手动拷文件。
> 那里面有个必须注意的点：上传不会删除旧文件，根目录的 `txtToWorldbook.js` 要手动删掉。

---

## 装之前

**1. 备份旧的插件文件夹**

找到你现在的插件目录，整个复制一份放到别处。出问题时把它换回去就能恢复原样。

**2. 关掉 SillyTavern 页面**

装的过程中别开着，装完再重新打开。

---

## 方式一：电脑（推荐）

1. 解压 `Novel-Auto-Generator-v4.0.1.zip`
2. 打开 SillyTavern 目录，进入：
   ```
   SillyTavern/public/scripts/extensions/third-party/
   ```
3. 找到你现在的插件文件夹（名字里通常带 `Novel-Auto-Generator` 或 `novel-auto-generator`）
4. **把里面的东西全部删掉**，然后把解压出来的所有文件和文件夹放进去
5. 重启 SillyTavern（关掉命令行窗口，重新运行启动脚本）
6. 浏览器里按 `Ctrl + F5` 强制刷新

**关键点：文件夹名字不要改。** 保持和原来一模一样，改了可能识别不到。

放进去之后，文件夹里应该长这样：

```
你的插件文件夹/
├── index.js
├── manifest.json
├── style.css
├── epubToTxt.js
├── worldbookExport.js
└── txtToWorldbook/          ← 这个文件夹里有 80 多个文件，都要在
    ├── main.js
    ├── app/
    ├── core/
    ├── infra/
    ├── locales/
    ├── services/
    ├── styles/
    └── ui/
```

如果 `txtToWorldbook/` 文件夹是空的或者只有几个文件，说明没解压完整，重来一次。

包里还有 `tests/`、`tools/`、`node_modules/` 三个文件夹和一些配置文件，那是开发和验证用的，**插件运行完全用不到**。留着不影响任何功能，嫌占地方也可以删掉——但下次要我帮你改代码时，留着能省一轮来回。

---

## 方式二：手机

手机上装稍微麻烦，因为要解压一整个带子目录的压缩包。

**如果你是用手机连电脑上的 SillyTavern（局域网访问）**
→ 直接用上面的电脑方式装，手机这边只要刷新浏览器就行。

**如果 SillyTavern 就跑在手机上（Termux 之类）：**

1. 装一个支持解压到指定目录的文件管理器（MT管理器、Zarchiver 都行）
2. 把 zip 传到手机
3. 找到插件目录，路径大概是：
   ```
   .../SillyTavern/public/scripts/extensions/third-party/你的插件名/
   ```
4. **先把这个文件夹里的内容全部删除**
5. 用文件管理器把 zip **解压到这个文件夹**
   - 注意：要"解压到当前目录"，不是"解压到新建文件夹"
   - 解压完检查一下，`index.js` 应该直接就在这一层，而不是嵌在又一层文件夹里面
6. 重启 SillyTavern
7. 浏览器长按刷新按钮，选"清空缓存并硬性重新加载"；或者在设置里清一下浏览器缓存

**手机上最容易出的错**：解压后多套了一层文件夹，变成 `你的插件名/Novel-Auto-Generator-v4.0.1/index.js`。这样是不行的，要把里面那层的东西提上来。

---

## 装完先测这五步

按顺序做，哪一步不对就停在哪一步，把现象告诉我。

**第 1 步 · 能不能打开**
扩展面板里找到「📚 文件转换工具」，点开应该有三个按钮：TXT转世界书、EPUB转TXT、世界书导出。
→ 面板整个不见了，或者点了没反应，说明文件没放对或者没放全。

**第 2 步 · 主界面能不能打开**
点「📚 TXT转世界书」，弹窗应该正常出来。
→ 白屏或者报错，按 F12 打开控制台，把红色报错截图给我。

**第 3 步 · 新增的输入框在不在**
在设置区找「API超时(秒)」，它旁边现在应该多了两个框：**温度** 和 **最大输出**。
→ 没有，说明加载的还是旧版缓存，再硬刷新一次。

**第 4 步 · 实时输出**
随便传个小 txt 跑一章。点开「👁️ 实时输出」面板，应该能看到 AI 的字一个个蹦出来。
→ 这是这版最直观的变化，以前这个面板一直是空的。
→ 注意：并行处理时只有第一个开跑的章节会逐字显示，其余的只报进度，这是故意的（三股字流挤在一起没法看）。

**第 5 步 · 导入酒馆预设**
展开「💬 消息链配置」，点「📥 导入酒馆预设」，选一个你的预设 json。
应该先弹出一个解析摘要让你确认，确认后消息链会按预设铺开。

---

## 出问题怎么办

**白屏 / 插件不见了**
99% 是文件没放全，或者多套了一层文件夹。先按上面的目录结构对一遍。

**报错但不知道是啥**
电脑上按 F12，手机上用 Kiwi 浏览器或者电脑远程调试，找 Console 里的红色报错，截图给我。报错的第一行通常就写明了是哪个文件出问题。

**想退回旧版**
把备份的文件夹换回去就行。你说过没有历史数据，所以不用担心数据丢失。

---

## 这版和你之前那份的主要区别

**你原来的功能一个没少**，EPUB转TXT、世界书导出、导入更新章节、导入酒馆预设都在。

新增的：

- **实时看到 AI 输出**（以前那个面板是空的，因为代码里根本没往里写东西）
- **温度和最大输出可以在界面上改**（以前写死在代码里，中转有输出长度限制的话没法调）
- **超时问题修好了**：改成"只要还在传数据就不断"，不再是跑够几百秒就必挂
- **Anthropic 通道能用了**（作者版本里这个通道其实是坏的，缺了浏览器直连必需的请求头，而且 system 消息格式不对）
- 手动合并、增量导出、Prompt 模板可视化编辑
- JSON 被截断时能自动修复（长章节很实用）
- AI 返回空结果时会重试，不再当成功
- 处理时自动存盘，刷新页面不丢进度
- 剩余时间预估
- 历史版本对比是真的逐行 diff 了

去掉的：

- DeepSeek 和 Gemini代理两个选项（你说不用）
- 如果你的设置里残留着这两个值，会自动落回 OpenAI兼容，不会报错

改了默认值：

- 默认不再勾选酒馆API，默认走自定义API + OpenAI兼容
